export interface DeathInterface {
  cause: string; // причина смерті
  death: string; // кого вбили
  death_id: number;
  episode: number;
  last_words: string;
  number_of_deaths: number; // кількість попередніх вбивст
  responsible: string; // вбивця
  season: number;
}
